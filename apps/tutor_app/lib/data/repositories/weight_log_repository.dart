import 'package:supabase_flutter/supabase_flutter.dart';

/// Stub — será implementado na feature logs/weight.
final class WeightLogRepository {
  const WeightLogRepository(SupabaseClient client) : _client = client;
  // ignore: unused_field
  final SupabaseClient _client;
}
