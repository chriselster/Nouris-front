import 'package:supabase_flutter/supabase_flutter.dart';

/// Stub — será implementado na feature diet.
final class DietRepository {
  const DietRepository(SupabaseClient client) : _client = client;
  // ignore: unused_field
  final SupabaseClient _client;
}
