import 'package:flutter/material.dart';

/// Scanner de QR Code para vinculação do pet ao veterinário.
/// Stub — será implementado na feature pet_profile/auth.
final class LinkQrPage extends StatelessWidget {
  const LinkQrPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vincular ao Veterinário')),
      body: const Center(child: Text('QR Scanner — Em construção')),
    );
  }
}
