import 'package:flutter/material.dart';

/// Wizard de onboarding: criação do primeiro pet do tutor.
/// Stub — será implementado na feature pet_profile.
final class OnboardingPetPage extends StatelessWidget {
  const OnboardingPetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cadastrar seu Pet')),
      body: const Center(child: Text('Onboarding — Em construção')),
    );
  }
}
